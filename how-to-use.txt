How to use the downloaded files

1) Start the server in the 'Server-hotels' folder with the command "node index.js" (it will run at localhost: 5000)
2) Run "npm install" in the extracted folder 'hotel-system'
3) Run "npm start" to view the project
